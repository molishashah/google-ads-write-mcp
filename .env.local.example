# Base URL of your deployment (no trailing slash)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ─────────────────────────────────────────────────────────────
# Layer 1: Google Ads API access
# ─────────────────────────────────────────────────────────────

# Developer token from your Google Ads manager account
GOOGLE_ADS_DEVELOPER_TOKEN=your-developer-token

# Manager (MCC) account ID, no hyphens.
# Required when operating customer accounts under an MCC.
GOOGLE_ADS_LOGIN_CUSTOMER_ID=1234567890

# ─────────────────────────────────────────────────────────────
# Layer 1 + Layer 2: Google OAuth (single client for both)
#
# Per-user refresh tokens are minted via /api/auth and used to
# call the Google Ads API on behalf of each team member.
# The same OAuth client also gates sign-in to this MCP server.
#
# IMPORTANT: the OAuth consent screen MUST include the
# https://www.googleapis.com/auth/adwords scope.
# ─────────────────────────────────────────────────────────────
GOOGLE_CLIENT_ID=your-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-oauth-client-secret

# ─────────────────────────────────────────────────────────────
# Layer 2: JWT signing for the MCP auth handshake
# Generate with: openssl rand -hex 32
# ─────────────────────────────────────────────────────────────
JWT_SECRET=generate-a-random-secret-here

# Optional: restrict sign-in to a specific Google Workspace domain
ALLOWED_DOMAIN=yourcompany.com
